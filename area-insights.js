(()=>{const GROUPS={'Life with God':['Prayer','Scripture','Solitude','Fasting','Sabbath'],'Life with Others':['Community','Generosity','Witness','Service'],'Life with Self':['Environment','Use of Time','Sleep','Physical Health','Inner Life']};const stage=v=>v<2?'Emerging':v<3?'Establishing':v<4?'Deepening':'Sustaining';const stageLead={Emerging:'This area appears to be opening up rather than firmly established.',Establishing:'There are meaningful signs of growth here, though the underlying rhythms are still taking shape.',Deepening:'This area is becoming a significant part of how your life is being formed.',Sustaining:'This area appears woven into the ordinary pattern of your life and may offer strength for growth elsewhere.'};function scoreMap(){const out={};document.querySelectorAll('#domains .domain').forEach(card=>{const name=card.querySelector('.top b')?.textContent?.trim()||card.querySelector('b')?.textContent?.trim();const fill=card.querySelector('.bar > div');if(!name||!fill)return;const w=parseFloat(fill.style.width||getComputedStyle(fill).width);let pct=parseFloat(fill.style.width);if(!Number.isFinite(pct)){const bar=fill.parentElement?.getBoundingClientRect().width||0;const fw=fill.getBoundingClientRect().width||0;pct=bar?fw/bar*100:50}out[name]=1+(pct/100)*4});return out}function describe(name,vals){vals=vals.filter(x=>Number.isFinite(x.v)).sort((a,b)=>b.v-a.v);if(!vals.length)return'';const mean=vals.reduce((n,x)=>n+x.v,0)/vals.length,st=stage(mean),hi=vals[0],second=vals[1],lo=vals[vals.length-1],spread=hi.v-lo.v;const strength=second&&Math.abs(hi.v-second.v)<.45?`${hi.d} and ${second.d} currently appear to be the strongest signals within this area.`:`${hi.d} currently appears to be the clearest strength within this area.`;const contrast=spread>=1.15?`${lo.d} sits noticeably lower than the strongest domains. That contrast is important: the overall ${st.toLowerCase()} description is a summary of a varied landscape, not a claim that every part of this area is at the same place.`:spread>=.6?`${lo.d} currently has somewhat less room than ${hi.d}, giving this area a gently uneven shape rather than one uniform level.`:`The domains sit relatively close together, suggesting a fairly coherent pattern across this area rather than one domain carrying the result.`;const meaning=name==='Life with God'?`Taken together, this suggests the particular ways you are currently making room for God, receiving from him and allowing relationship with him to shape ordinary life.`:name==='Life with Others'?`Taken together, this gives a picture of how life with Jesus is becoming embodied through belonging, generosity, witness and service rather than remaining private conviction.`:`Taken together, this reflects the everyday conditions — attention, pace, rest, embodiment and inner awareness — that can support or constrain your availability to God and other people.`;return `<strong>${stageLead[st]}</strong> ${strength} ${contrast} ${meaning}`}function apply(){const areas=document.getElementById('areas');if(!areas||!areas.children.length)return;const scores=scoreMap();[...areas.children].forEach(card=>{const name=card.querySelector('.ey')?.textContent?.trim();if(!GROUPS[name])return;const vals=GROUPS[name].map(d=>({d,v:scores[d]}));let p=card.querySelector('.area-insight');if(!p){p=document.createElement('p');p.className='area-insight';card.appendChild(p)}p.innerHTML=describe(name,vals)});}const obs=new MutationObserver(()=>apply());obs.observe(document.documentElement,{subtree:true,childList:true});document.addEventListener('DOMContentLoaded',apply);window.FormationAreaInsights={apply};})();