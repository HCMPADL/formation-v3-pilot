name: Release hardening

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: release-hardening-${{ github.ref }}
  cancel-in-progress: true

jobs:
  regression:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium webkit
      - name: Unit tests
        timeout-minutes: 2
        run: npm run test:unit
      - name: Critical quick journey — desktop Chromium
        timeout-minutes: 3
        env:
          DEBUG: pw:test
        run: npx playwright test tests/release-hardening.spec.js --project=desktop-chromium --grep "quick assessment completes"
      - name: Cross-browser smoke — desktop Chromium
        timeout-minutes: 2
        run: npx playwright test tests/release-hardening.spec.js --project=desktop-chromium --grep "cross-browser smoke"
      - name: Cross-browser smoke — mobile Chromium
        timeout-minutes: 2
        run: npx playwright test tests/release-hardening.spec.js --project=mobile-chromium --grep "cross-browser smoke"
      - name: Cross-browser smoke — mobile WebKit
        timeout-minutes: 2
        run: npx playwright test tests/release-hardening.spec.js --project=mobile-webkit --grep "cross-browser smoke"
      - name: Upload Playwright diagnostics
        if: ${{ always() && !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: playwright-diagnostics-${{ github.run_attempt }}
          path: |
            playwright-report/
            test-results/
          if-no-files-found: ignore
          retention-days: 7
